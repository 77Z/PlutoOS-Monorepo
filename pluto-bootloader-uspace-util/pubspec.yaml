name: pluto_bootloader_backend
description: Bootloader backend for RAUC
version: 1.0.0

environment:
  sdk: ^3.9.2

dependencies:
  path: ^1.9.0
  posix: ^6.0.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

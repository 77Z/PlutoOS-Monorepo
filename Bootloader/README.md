# PlutoOS Bootloader

This is the most up to date (V4) bootloader for starting up PlutoOS.
It's a custom grub implementation that builds to a single EFI binary
and has to decide whether to boot slot A or B.

### Building

```bash
./buildBootloader.sh
```
